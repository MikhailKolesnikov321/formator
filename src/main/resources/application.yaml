server:
  port: 8081
  error:
    whitelabel:
      enabled: false

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/formator
    username: postgres
    password: dunice
    driver-class-name: org.postgresql.Driver
  application:
    name: formator
  jooq:
    sql-dialect: POSTGRES

  liquibase:
    enabled: true
    change-log: classpath:/db.changelog/main.xml

management:
  endpoints:
    web:
      exposure:
        include: '*'

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: "neeeeeewwwwwwwwwKeeeeeeeeySecreeeeet"
  lifeTime: 3600000
